extends layout

block content
  h1= title

  if error
    p Dynamic Content
  else
    p Inventory stats:

    ul
      li #[strong Items: ] !{data.items}
      li #[strong Categories: ] !{data.category}
